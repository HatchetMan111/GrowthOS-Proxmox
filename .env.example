# Wird vom Installer nach .env kopiert. ADMIN_PASSWORD wird dabei automatisch
# ersetzt (zufällig generiert), sofern nicht per ADMIN_PASSWORD=... beim
# Installer-Aufruf vorgegeben.
ADMIN_PASSWORD=change-me
